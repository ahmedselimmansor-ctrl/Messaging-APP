# Rendered manifests are build output, produced by
# scripts/render-manifests.sh. They are regenerated by Cloud Build before each
# release and must never be committed — a stale checked-in copy would deploy
# while looking like it matched the overlays.
*.yaml
