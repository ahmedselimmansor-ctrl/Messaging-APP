apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - migrations.yaml
  - maintenance.yaml
  # Generates the cassandra-schema ConfigMap from the authoritative CQL.
  - ../../../db/cassandra
